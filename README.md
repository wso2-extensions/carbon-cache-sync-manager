# carbon-cache-sync-manager
This repository contains extensions aimed at enhancing the cache invalidation mechanism to support multiple Identity Server (IS) cluster deployments in Active-Active mode.
